#include<iostream>
using namespace std;

int main() {
    int i = 1;  // Initialize the counter
    
    while(i <= 100) {  // Condition to run the loop
        cout << i << " ";  // Print the current value of i
        i++;  // Increment the counter
    }
    
    return 0;
}
